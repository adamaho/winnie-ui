<div class="accents">
  {
    [
      "red",
      "orange",
      "amber",
      "yellow",
      "lime",
      "green",
      "sky",
      "blue",
      "indigo",
      "purple",
      "fuchsia",
      "pink",
      "brown",
    ].map((accent) => {
      return (
        <span class="accent" data-accent-color={accent}>
          {/* {accent} */}
        </span>
      );
    })
  }
</div>

<style>
  .accents {
    display: grid;
    grid-template-row: repeat(12, 1fr);
    gap: var(--wui-space-1);
    height: 100svh;
    left: 0;
    margin: var(--wui-space-1);
    position: fixed;
    top: 0;
    width: calc(32px * var(--wui-scale));

    .accent {
      background: var(--wui-color-accent-7);
      display: flex;
      flex-direction: column;
      justify-content: flex-end;
      width: calc(32px * var(--wui-scale));
      transition-property: width, background;
      transition-duration: var(--wui-duration-1);
      transition-timing-function: linear(0, 0.5 50%, 0.75 80%, 1);

      &:hover {
        width: calc(90px * var(--wui-scale));
      }

      :hover + & {
        width: calc(45px * var(--wui-scale));
      }

      &:has(+ :hover) {
        width: calc(45px * var(--wui-scale));
      }
    }
  }
</style>
