---
title: Dialog
description: An interactive element that opens on top of the page content.
---

import Demo from "~/components/demo/demo.astro";

import DialogSize from "~/demos/css/dialog-size.astro";
import DialogWithClose from "~/demos/css/dialog-with-close.astro";
import DialogWithContent from "~/demos/css/dialog-with-content.astro";
import DialogWithField from "~/demos/css/dialog-with-field.astro";
import DialogWithHeader from "~/demos/css/dialog-with-header.astro";
import Dialog from "~/demos/css/dialog.astro";

<Demo>
  <Dialog />
  <Fragment slot="footer">
    ```tsx
    <dialog data-component="dialog" data-size="sm">
      <h3 data-slot="title">Delete Dish</h3>
      <p data-slot="description">
        Are you sure you want to delete Margarita Pizza?
      </p>
      <footer data-component="footer">
        <button
          data-component="button"
          data-slot="cancel"
          data-accent-color="grey"
          data-variant="4"
        >
          Cancel
        </button>
        <button
          data-component="button"
          data-slot="action"
          data-accent-color="red"
          data-variant="1"
        >
          Delete
        </button>
      </footer>
    </dialog>
    ```
  </Fragment>
</Demo>

## Import

Import just the dialog styles.

```css
@import "@winnie-ui/css/spatial/dialog";
@import "@winnie-ui/css/visual/dialog";
```

## Anatomy

Attach the appropriate `data` attributes in the structure below.

```html
<dialog data-component="dialog">
  <header data-component="header">
    <button data-slot="close" />
  </header>
  <h3 data-slot="title" />
  <p data-slot="description" />
  <div data-component="content" />
  <footer data-component="footer">
    <button data-slot="cancel" />
    <button data-slot="action" />
  </footer>
</dialog>
```

## Examples

### Size

Use the `data-size` attribute to change the max width of the dialog.

<Demo>
  <DialogSize />
  <Fragment slot="footer">
    ```tsx ins=data-size="<size>"
    <dialog data-component="dialog" data-size="<size>">
      ...
    </dialog>
    ```
  </Fragment>
</Demo>

### Header

Use the `dialog-header` component to add a header to the dialog.

<Demo>
  <DialogWithHeader />
  <Fragment slot="footer">
    ```tsx mark={2}
    <dialog data-component="dialog">
      <header data-component="dialog-header">...</header>
      ...
    </dialog>
    ```
  </Fragment>
</Demo>

### Close

Use the `close` slot component to add a close button to the top right of the dialog.

<Demo>
  <DialogWithClose />
  <Fragment slot="footer">
    ```tsx mark={4} mark={5} mark={6}
    <dialog data-component="dialog">
      <header>
        ...
        <button data-component="button" data-slot="close">
          <i class="x"></i>
        </button>
      </header>
      ...
    </dialog>
    ```
  </Fragment>
</Demo>

### Content

Use the `dialog-content` component to add content, like a form, to the dialog.

<Demo>
  <DialogWithContent />
  <Fragment slot="footer">
    ```tsx mark={3}
    <dialog data-component="dialog">
      ...
      <div data-component="dialog-content">...</header>
      ...
    </dialog>
    ```
  </Fragment>
</Demo>

### Fields

Use fields like `input` and `textarea` as the `title` and `description` slots.

<Demo>
  <DialogWithField />
  <Fragment slot="footer">
    ```tsx mark={3} mark={4}
    <dialog data-component="dialog">
      ...
      <input data-slot="title" />
      <textarea data-slot="description" />
      ...
    </dialog>
    ```
  </Fragment>
</Demo>

## Reference

### dialog

Groups all of the parts of the dialog together.

| Attribute   | Value                  |
| ----------- | ---------------------- |
| `data-size` | `"sm" \| "md" \| "lg"` |

| Variable                               |
| -------------------------------------- |
| `--wui-dialog-background`              |
| `--wui-dialog-border`                  |
| `--wui-dialog-box-shadow`              |
| `--wui-dialog-color`                   |
| `--wui-dialog-header-border-bottom`    |
| `--wui-dialog-title-color`             |
| `--wui-dialog-title-font-weight`       |
| `--wui-dialog-description-color`       |
| `--wui-dialog-description-font-weight` |
| `--wui-dialog-footer-border-top`       |
