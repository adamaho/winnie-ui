---
title: textarea
description: Captures multiline user input.
---

import Demo from "~/components/demo/demo.astro";

import TextareaDisabled from "~/demos/css/textarea-disabled.astro";
import TextareaInvalid from "~/demos/css/textarea-invalid.astro";
import Textarea from "~/demos/css/textarea.astro";

<Demo>
  <Textarea />
  <Fragment slot="footer">
    ```tsx
    <textarea data-component="textarea" />
    ```
  </Fragment>
</Demo>

## Import

Import just the textarea styles.

```css
@import "@winnie-ui/css/spatial/textarea";
@import "@winnie-ui/css/visual/textarea";
```

## Anatomy

Attach the appropriate `data` attributes in the structure below.

```tsx
<textarea data-component="textarea" />
```

## Examples

### Disabled

Use the `disabled` attribute to prevent user interaction.

<Demo>
  <TextareaDisabled />
  <Fragment slot="footer">
    ```tsx ins=disabled
    <textarea data-component="textarea" disabled />
    ```
  </Fragment>
</Demo>

### Invalid

`textarea` will respect the `:invalid` selector when it is used in a form. Use the `data-invalid` attribute to manually control the invalid state of the `texarea`.

<Demo>
  <TextareaInvalid />
  <Fragment slot="footer">
    ```tsx ins=data-invalid="true"
    <textarea data-component="textarea" data-invalid="true" />
    ```
  </Fragment>
</Demo>

## Reference

### textarea

Represents the textarea element.

| Attribute       | Value     |
| --------------- | --------- |
| `data-disabled` | `boolean` |
| `data-invalid`  | `boolean` |

| Variable |
| -------- |
