---
title: button
description: Trigger an event, action or operation.
---

import Demo from "~/components/demo/demo.astro";

import ButtonColor from "~/demos/css/button-color.astro";
import ButtonDisabled from "~/demos/css/button-disabled.astro";
import ButtonFullWidth from "~/demos/css/button-full-width.astro";
import ButtonIconOnly from "~/demos/css/button-icon-only.astro";
import ButtonPending from "~/demos/css/button-pending.astro";
import ButtonRadius from "~/demos/css/button-radius.astro";
import ButtonShortcuts from "~/demos/css/button-shortcuts.astro";
import ButtonSize from "~/demos/css/button-size.astro";
import ButtonVariant from "~/demos/css/button-variant.astro";
import ButtonWithIcon from "~/demos/css/button-with-icon.astro";
import Button from "~/demos/css/button.astro";

<Demo>
  <Button />
  <Fragment slot="footer">
    ```tsx
    <button data-component="button">
      <i class="fork-knife" data-slot="icon"></i>
      <span data-slot="label">New Dish</span>
    <button>
    ```
  </Fragment>
</Demo>

## Import

Import just the button styles.

```css
@import "@winnie-ui/css/spatial/button";
@import "@winnie-ui/css/visual/button";
```

## Anatomy

Attach the appropriate `data` attributes in the structure below.

```html
<button data-component="button">
  <i data-slot="icon"></i>
  <span data-slot="label"></span>
  <span data-slot="pending"></span>
  <kbd data-slot="shortcut">
    <kbd data-component="kbd" />
  </kbd>
</button>
```

## Examples

### Size

Use the `data-size` attribute to change the size.

<Demo>
  <ButtonSize />
  <Fragment slot="footer">
    ```tsx ins=data-size="<size>"
    <button data-component="button" data-size="<size>">
      <span data-slot="label">Edit Dish</span>
    </button>
    ```
  </Fragment>
</Demo>

### Variant

Use the `data-variant` attribute to change look of the button.

<Demo>
  <ButtonVariant />
  <Fragment slot="footer">
    ```tsx ins=data-variant="<variant>"
    <button data-component="button" data-variant="<variant>">
      <span data-slot="label">Edit Dish</span>
    </button>
    ```
  </Fragment>
</Demo>

### Radius

Use the `data-radius` attribute to change the radius.

<Demo>
  <ButtonRadius />
  <Fragment slot="footer">
    ```tsx ins=data-radius="<radius>"
    <button data-component="button" data-radius="<radius>">
      <span data-slot="label">Edit Dish</span>
    </button>
    ```
  </Fragment>
</Demo>

### Color

Use the `data-accent-color` attribute to modify the color of the button. By default the button will inherit the accent color from the closest parent that specifies the `data-accent-color` attribute.

<Demo>
  <ButtonColor />
  <Fragment slot="footer">
    ```tsx ins=data-accent-color="<color>"
    <button data-component="button" data-accent-color="<color>">
      <span data-slot="label">Edit Dish</span>
    </button>
    ```
  </Fragment>
</Demo>

### Icon

Add an icon with the `icon` slot attribute as a child of the button.

<Demo>
  <ButtonWithIcon />
  <Fragment slot="footer">
    ```tsx ins=data-slot="icon" 
    <button data-component="button">
      <i class="fork-knife" data-slot="icon"></i>
      <span data-component="text">New Dish</span>
    <button>
    ```
  </Fragment>
</Demo>

### Icon Only

Create an icon button by only passing an icon with the `icon` slot attribute as a child of the button.

<Demo>
  <ButtonIconOnly />
  <Fragment slot="footer">
    ```tsx ins=data-slot="icon"
    <button data-component="button">
      <i class="fork-knife" data-slot="icon"></i>
    <button>
    ```
  </Fragment>
</Demo>

### Shortcuts

Add shortcut identifiers to your button using the `shortcut` slot and the `kbd` component. These shortcuts typically only apply to users on screens larger than 768px, so we hide them for any viewport width smaller than that.

<Demo>
  <ButtonShortcuts />
  <Fragment slot="footer">
    ```tsx ins=data-slot="shortcut"
    <button data-component="button">
      ...
      <kbd data-slot="shortcut">
        <kbd data-component="kbd">E</kbd>
      </kbd>
    <button>
    ```
  </Fragment>
</Demo>

### Pending

If a child of the button is rendered with the `pending` attribute, any text and icons rendered in the button will be hidden and the `pending` slot will be rendered.

<Demo>
  <ButtonPending />
  <Fragment slot="footer">
    ```tsx mark={2 - 4}
    <button data-component="button">
      <span data-slot="pending">
        <i class="loader"></i>
      </span>
      <span data-slot="label">Edit Dish</span>
    </button>
    ```
  </Fragment>
</Demo>

### Disabled

Use the `disabled` attribute to prevent user interaction.

<Demo>
  <ButtonDisabled />
  <Fragment slot="footer">
    ```tsx mark=disabled
    <button data-component="button" disabled>
      <span data-slot="label">Edit Dish</span>
    </button>
    ```
  </Fragment>
</Demo>

### Full Width

Use the `data-width` attribute to modify the overall width of the button.

<Demo>
  <ButtonFullWidth />
  <Fragment slot="footer">
    ```tsx ins=data-width="full"
    <button data-component="button" data-width="full">
      <i class="fork-knife" data-slot="icon"></i>
      <span data-slot="slot">New Dish</span>
    </button>
    ```
  </Fragment>
</Demo>

## Reference

### button

Groups all of the parts of the button together.

| Attribute           | Value                                                                                           |
| ------------------- | ----------------------------------------------------------------------------------------------- |
| `data-accent-color` | `"red" \| "orange" \| "yellow" \| "green" \| "blue" \| "purple" \| "pink" \| "grey" \| "brand"` |
| `data-radius`       | `"none" \| "sm" \| "md" \| "lg" \| "round"`                                                     |
| `data-size`         | `"sm" \| "md" \| "lg"`                                                                          |
| `data-variant`      | `"1" \| "2" \| "3" \| "4"`                                                                      |
| `data-width`        | `"full"`                                                                                        |

| Modifier   | Description                                         |
| ---------- | --------------------------------------------------- |
| `hovered`  | When the button is hovered with a mouse             |
| `pressed`  | When the button is pressed with a mouse or keyboard |
| `disabled` | When the button prevents user interaction           |

| Variable                                             |
| ---------------------------------------------------- |
| `--wui-button-<data-variant>-background-<?modifier>` |
| `--wui-button-<data-variant>-border-<?modifier>`     |
| `--wui-button-<data-variant>-box-shadow-<?modifier>` |
| `--wui-button-<data-variant>-color-<?modifier>`      |

### kbd

Single key in the `shortcut` slot.

| Modifier   | Description                               |
| ---------- | ----------------------------------------- |
| `disabled` | When the button prevents user interaction |

| Variable                                                 |
| -------------------------------------------------------- |
| `--wui-button-kbd-<data-variant>-background-<?modifier>` |
| `--wui-button-kbd-<data-variant>-border-<?modifier>`     |
| `--wui-button-kbd-<data-variant>-box-shadow-<?modifier>` |
| `--wui-button-kbd-<data-variant>-color-<?modifier>`      |
