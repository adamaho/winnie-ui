---
title: Input
description: Captures user input.
---

import Demo from "~/components/demo/demo.astro";
import Aside from "~/components/markdown/aside.astro";

import InputHero from "./_components/input/input-hero.astro";
import InputIcon from "./_components/input/input-icon.astro";
import InputRadius from "./_components/input/input-radius.astro";
import InputSize from "./_components/input/input-size.astro";

<Demo>
  <InputHero />
  <Fragment slot="footer">
    ```tsx
    <div data-component="group">
      <i class="search" data-slot="icon"></i>
      <input data-component="input" data-slot="control" />
    </div>
    ```
  </Fragment>
</Demo>

## Import

Import just the input styles.

```css
@import "@winnie-ui/css/spatial/input";
```

## Anatomy

Attach the appropriate `data-component` attributes in the structure below.

```html
<input data-component="input" />
```

## Examples

### Size

Use the `data-size` attribute to change the size.

<Demo>
  <InputSize />
  <Fragment slot="footer">
    ```tsx ins=data-size="1" ins=data-size="2" ins=data-size="3"
    <input data-component="input" data-size="1" />
    <input data-component="input" data-size="2" />
    <input data-component="input" data-size="3" />
    ```
  </Fragment>
</Demo>

### Radius

Use the `data-radius` attribute to change the size.

<Demo>
  <InputRadius />
  <Fragment slot="footer">
    ```tsx ins=data-radius="none" ins=data-radius="2" ins=data-radius="round"
    <input data-component="input" data-radius="none" />
    <input data-component="input" data-radius="2" />
    <input data-component="input" data-radius="round" />
    ```
  </Fragment>
</Demo>

### With Icon

Wrap the input in an element with the `data-component="group"` attribute define and pass the input and icons as children.

<Aside type="info">
  If you are just importing the input styles, you will need to also import the group styles for this to work.

```css
@import "@winnie-ui/css/spatial/input-group";
```

</Aside>

<Demo>
  <InputIcon />
  <Fragment slot="footer">
    ```tsx ins=data-slot="icon"
    <div data-component="input-group">
      <input data-component="input" data-slot="input" />
      <i class="chevron-up-down" data-slot="icon"></i>
    </div>
    ```
  </Fragment>
</Demo>

## Reference

Below is a list of the available input `data-` attributes available for input.

| Attribute     | Value                                       |
| ------------- | ------------------------------------------- |
| `data-radius` | `"none" \| "1" \| "2" \| "3" \| "round"` |
| `data-size`   | `"1" \| "2" \| "3"`                      |
